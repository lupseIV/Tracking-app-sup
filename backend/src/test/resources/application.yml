# Full test configuration — this file shadows src/main/resources/application.yml,
# it does not merge with it.
spring:
  application:
    name: supplement-tracker
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
